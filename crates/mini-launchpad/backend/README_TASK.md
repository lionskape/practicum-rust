# Backend Task

Это учебная версия `backend/` для студентов. В этой копии задание уже решено.

Что было исправлено:
- `to_fixed_6` преобразует десятичную цену в fixed-point формат с 6 знаками;
- unit-тест на лишние дробные знаки проверяет усечение, а не округление;
- `Cargo.toml` смотрит на фактическую локальную папку `../program`.

Как запускать:

```bash
cd backend
cargo test
```

Ожидаемый результат: все unit-тесты проходят.
